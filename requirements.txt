######## Core app ########
# Streamlit: 1.37.0+ includes fix for the Windows path traversal issue (CVE-2024-42474).
# 1.39.0 is newer and still supported, so you can safely keep it.
streamlit==1.39.0

# pandas: avoid versions <=2.2.2, which are affected by CVE-2024-42992 (arbitrary file read).
# Use the latest 2.2.x that is available to you; Defender usually just checks that you’re
# on a non-vulnerable minor/patch. Example:
pandas==2.2.3

# python-dotenv: no widely reported recent CVEs; keep within stable major.
python-dotenv>=1.0,<2

# requests: keep in the latest 2.32.x line to pick up security fixes.
requests>=2.32,<3

# urllib3: CVEs like CVE-2025-66418/66471 affect <2.6.0; use 2.6.0 or later.
urllib3>=2.6,<3
